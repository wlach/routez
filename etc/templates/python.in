#!/bin/sh

export LD_LIBRARY_PATH="${buildout:directory}/develop-eggs/rtree:${libspatialindex:location}/lib:${libroutez:location}/lib:${neocoder:location}/lib:$LD_LIBRARY_PATH"
export DYLD_LIBRARY_PATH="${buildout:directory}/develop-eggs/rtree:${libspatialindex:location}/lib:${libroutez:location}/lib:${neocoder:location}/lib:$DYLD_LIBRARY_PATH"
export PYTHONPATH="${buildout:directory}/src/routez:$PYTHONPATH"

${buildout:directory}/bin/_python $@
